import {FoodItemDietType} from '../../../../data/remote/foodData/FoodItemDietType';

/**
 * Props type to be used for the component DietaryTypeIcon
 */
export type DietaryTypeIconPropsType = {
  dietaryType: FoodItemDietType;
};
